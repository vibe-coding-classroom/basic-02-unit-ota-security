# 安全審計清單 (Security Audit Checklist)

學員需在此記錄各項安全測試結果。

## 1. 身份驗證測試 (Authentication Test)
*   **測試目的**: 驗證未授權使用者是否無法進入更新頁面。
*   **測試步驟**:
    1. 使用無痕視窗開啟 `/update`。
    2. 是否彈出 HTTP Basic Auth 對話框？ [ ] 是 / [ ] 否
    3. 輸入錯誤密碼，是否顯示 401 錯誤？ [ ] 是 / [ ] 否
*   **測試結果說明**:

## 2. 版本降級測試 (Anti-rollback Test)
*   **測試目的**: 確保系統無法刷入版本號低於目前的韌體。
*   **測試步驟**:
    1. 編譯一個版本號為 `0.9.0` 的韌體。
    2. 嘗試透過 OTA 上傳。
    3. 系統是否調用 `Update.abort()` 並顯示錯誤？ [ ] 是 / [ ] 否
*   **測試結果說明**:

## 3. 硬體型號鎖定測試 (Hardware Affinity Test)
*   **測試目的**: 防止韌體刷入不匹配的硬體架構。
*   **測試步驟**:
    1. 修改 `version.h` 中的 `HARDWARE_TARGET` 為非目前晶片的型號 (例如目前是 S3，改為 C3)。
    2. 嘗試上傳。
    3. 系統是否偵測到型號不符並拒絕更新？ [ ] 是 / [ ] 否
*   **測試結果說明**:
